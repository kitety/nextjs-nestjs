import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      <button type="button" className="btn btn-success">
        （成功）Success
      </button>
    </div>
  );
};

export default Home;
